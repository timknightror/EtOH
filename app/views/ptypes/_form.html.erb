<%= simple_form_for(@ptype) do |f| %>
  <% if @ptype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ptype.errors.count, "error") %> prohibited this Phone Type from being saved:</h2>

      <ul>
      <% @ptype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
    <%= f.input :name, :label => 'Phone Type' %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
